
var expect = chai.expect;

describe('MyFunction', function() {
    describe('#playTopCard', function() {
        it('should play the top card', function() {
            const newCard = new Card();
            expect(newCard.value).to.equal();
        });
    });
});